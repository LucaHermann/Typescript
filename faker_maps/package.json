{
  "name": "faker_maps",
  "version": "0.0.1",
  "description": "faker + maps API for a dumb app",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lhermann",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "@types/faker": "^5.1.4",
    "@types/googlemaps": "^3.40.3",
    "faker": "^5.1.0"
  }
}
